{
  "name": "@x-cli/version",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "bun run scripts/generate-version.ts",
    "generate:dev": "bun run scripts/generate-version.ts --dev",
    "test": "bunx --bun vitest run",
    "typecheck": "bun run scripts/generate-version.ts && tsc --noEmit"
  }
}